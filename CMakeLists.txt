cmake_minimum_required(VERSION 3.5)
project( FINAL_GRAFICA )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( FINAL_GRAFICA src/main.cpp )
target_link_libraries( FINAL_GRAFICA ${OpenCV_LIBS} )
